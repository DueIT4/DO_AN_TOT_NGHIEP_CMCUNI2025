name: mobile_web_flutter
description: A Flutter web app for AI Plant Health
publish_to: 'none' # không publish lên pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Gói cơ bản
  cupertino_icons: ^1.0.8
  
  # Gọi API backend
  http: ^1.2.2
  google_sign_in: ^6.2.1
  flutter_facebook_auth: ^7.0.1
  
  # Google Gemini AI
  google_generative_ai: ^0.4.3

  # Chọn ảnh (gallery/camera)
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  intl: ^0.19.0   # 👈 THÊM DÒNG NÀY
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
