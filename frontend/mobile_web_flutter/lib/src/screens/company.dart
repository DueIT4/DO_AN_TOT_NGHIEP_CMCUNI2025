import 'package:flutter/material.dart';
class CompanyScreen extends StatelessWidget { const CompanyScreen({super.key});
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Về công ty')), body: const Center(child: Text('Giới thiệu, sứ mệnh, đội ngũ…')));
}
